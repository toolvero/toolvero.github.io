# Toolvero v2.1.1 — Homepage Search Fix

All six v2.1 tools now use the same `.tool-item` search system as the original tools.

Test:
- dns prop → DNS Propagation Checker
- mac address → MAC Address Formatter
- ipv4 range → IPv4 Range Calculator
- status → HTTP Status Codes
- csv → JSON ↔ CSV
- compose validator → Docker Compose Validator
- clear query → 32 tools

Suggested commit:
`Fix Toolvero v2.1.1 - Integrate new tools into homepage search`
